# Pinned 2026-06-28 to known-good versions. Previously these were unpinned (>=),
# so a rebuild after Railway's builder updated pulled newer versions and broke the
# build. These versions match the working local env.
garminconnect==0.2.8
tabulate==0.9.0
pytest==8.4.2
anthropic==0.86.0
fastapi==0.128.8
uvicorn[standard]==0.39.0
sse-starlette==3.3.0
psycopg2-binary==2.9.11
pydantic==2.12.5
starlette==0.49.3
